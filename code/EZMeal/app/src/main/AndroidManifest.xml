<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.lsinf1225.ezmeal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:roundIcon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".SignInActivity">
           <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountActivity"></activity>
        <activity android:name=".TimerActivity"></activity>

        <activity android:name=".Etape_middle"></activity>
        <activity android:name=".menu"></activity>
        <activity android:name=".SearchResult"></activity>
        <activity android:name=".search"></activity>
        <activity android:name=".catalogue"></activity>
        <activity android:name=".suggest"></activity>
        <activity android:name=".AvisActivity"></activity>
    </application>

</manifest>